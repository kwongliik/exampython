# The following code contains errors.
# You need to do corrections on the code.
# Try to do corrections on online-python.com OR IDLE PYTHON before you commit the changes.

def calculation(a, b):
    multiplication = a + b
    division = a - b
    return a, b  # return multiple values separated by comma

result = calculations(30, 5) # Call the function to get the result
print(result) # Don't change the code. It shows the output as "(150, 6.0)"
