# The following code contains errors.
# You need to do corrections on the code.
# Try to do corrections on online-python.com OR IDLE PYTHON before you commit the changes.

# The output of the program is as follows:
# (150, 6.0)

def calculation(a, b):
    multiplication = a + b
    division = a - b
    # return multiple values separated by comma
    return a, b

# get result in tuple format
result = calculations(30, 5)
print(result) # Don't change this line code
